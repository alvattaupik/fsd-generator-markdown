# 1 Deskripsi Use Case

**Tabel 1: Deskripsi Use Case First Time Login User Kasir**

| Elemen Spesifikasi | Deskripsi Detail |
| --- | --- |
| ID Use Case | UC-WPOS-003 |
| Nama Use Case | First Time Login User Kasir |
| Penjelasan Singkat | Proses aktivasi akun Kasir saat pertama kali mengakses aplikasi Web POS menggunakan kredensial sementara, verifikasi OTP email via OTP Service, hingga pembaruan kata sandi sesuai kriteria standar CITRA dengan perlindungan anti-bypass. |
| Aktor Utama | Kasir |
| Aktor Pendukung | OTP Service, Email Service |
| Deskripsi Singkat | Kasir menginput email dan password sementara pada Halaman First Time Login Web POS. Sistem memvalidasi status binding perangkat, memverifikasi kredensial sementara (`login_type = 'REGULER'`, `login_status = 'INACTIVE'`, `first_time_login_status = 'REQUIRE_CHANGE_PASSWORD'`, `merchant_id` & `store_id` valid, serta `temporary_password_expired_at` masih berlaku), memicu OTP Service untuk mengirimkan kode OTP ke email terdaftar, memverifikasi OTP untuk membuat `otp_verification_token` anti-bypass di Redis Cache, serta memvalidasi password baru sesuai standar CITRA sebelum mengaktifkan akun (`login_status = 'ACTIVE'`, `first_time_login_status = 'COMPLETED'`, serta mengisi `password_expired_at = CURRENT_TIMESTAMP + 90 hari`) dan memberikan token sesi. |
| Pre-condition | 1. Aplikasi dan perangkat Web POS telah ter-binding serta ter-aktivasi pada toko/outlet (`store_id`) terkait.<br>2. Akun Kasir telah terdaftar pada tabel `mst_users` dengan `login_type = 'REGULER'`, serta memiliki asosiasi `merchant_id` dan `store_id` yang valid.<br>3. Akun Kasir berstatus `INACTIVE` dan `first_time_login_status = 'REQUIRE_CHANGE_PASSWORD'`.<br>4. Password sementara akun Kasir belum kedaluwarsa (`temporary_password_expired_at > CURRENT_TIMESTAMP`). |
| Post-condition | 1. Password sementara berhasil diganti dengan password baru yang memenuhi kriteria standar CITRA.<br>2. Status akun Kasir diperbarui pada `mst_users` menjadi `login_status = 'ACTIVE'`, `first_time_login_status = 'COMPLETED'`, `temporary_password_expired_at = NULL`, dan kolom `password_expired_at` diisi dengan masa berlaku 90 hari (`CURRENT_TIMESTAMP + 90 hari`).<br>3. Data sesi disimpan pada Redis Cache, dan Front-End Web POS mengarahkan Kasir ke Layar Utama Transaksi (POS Main Screen) sesuai `store_id` dan hak akses `role_id`. |
| Trigger | Kasir memasukkan email dan password sementara lalu menekan tombol "Login". |
| Nama Tabel | mst_users, mst_role_permissions, mst_role_menus, audit_logs |
| Integrasi | 1. **Redis Cache:** Penyimpanan temporary OTP, token anti-bypass (`otp_verification_token`), rate limit counter, dan session token.<br>2. **OTP Service:** Layanan pembuatan, validasi, dan manajemen siklus hidup kode OTP.<br>3. **Email Service:** Layanan pengiriman fisik email kode OTP ke alamat email terdaftar Kasir. |
| Asumsi | 1. Perangkat Web POS terhubung ke koneksi internet yang stabil dan memiliki status binding yang sah.<br>2. Kasir memiliki akses aktif ke alamat email terdaftar di sistem.<br>3. Layanan OTP Service, Email Service, dan Redis Cache beroperasi dengan stabil. |
| Keterbatasan | 1. First Time Login Kasir hanya dapat dilakukan pada perangkat Web POS yang sudah berstatus ter-binding dan ter-aktivasi.<br>2. Pembaruan password baru tidak dapat diproses tanpa memverifikasi kode OTP dan menyertakan `otp_verification_token` anti-bypass yang valid. |
| Aturan Bisnis/Sistem | 1. **Device Binding & Activation Requirement:** Aplikasi Web POS wajib memastikan perangkat telah ter-binding dan ter-aktivasi untuk `store_id` terkait sebelum memproses aktivasi.<br>2. **Temporary Credential Verification:** Login pertama wajib memverifikasi email dan password sementara pada `mst_users` yang memiliki `login_status = 'INACTIVE'` dan `first_time_login_status = 'REQUIRE_CHANGE_PASSWORD'`.<br>3. **Merchant & Store Association:** Kasir wajib terasosiasi dengan `merchant_id` dan `store_id` yang valid dan aktif pada `mst_users`.<br>4. **Temporary Password Validity:** Tanggal `temporary_password_expired_at` wajib lebih besar dari waktu saat ini (`> CURRENT_TIMESTAMP`).<br>5. **OTP Email Verification:** Setelah password sementara valid, Auth Service berintegrasi dengan **OTP Service** untuk memproduksi dan mengirimkan kode OTP dinamis (6 digit, masa berlaku 5 menit) ke email terdaftar via Email Service.<br>6. **OTP Rate Limit & Attempt Penalty:** Pengiriman OTP dibatasi maksimal 3 kali per menit. Percobaan verifikasi OTP salah maksimal 3 kali berturut-turut sebelum OTP dinyatakan kadaluarsa.<br>7. **Anti-Bypass OTP Token Mechanism:** Setelah verifikasi OTP berhasil, Auth Service menerbitkan `otp_verification_token` sekali pakai (*single-use*) yang disimpan pada **Redis Cache** (TTL 10 menit). Endpoint pembaruan password WAJIB memverifikasi dan mengonsumsi token ini. Apabila request pembaruan password dilakukan tanpa `otp_verification_token` yang sah, sistem menolak akses secara permanen (*Anti-Bypass Enforcement*).<br>8. **Standar Kriteria Password CITRA:**<br>&nbsp;&nbsp;- Panjang minimal 8 karakter.<br>&nbsp;&nbsp;- Wajib memuat kombinasi minimal 3 dari 4 kriteria: Karakter Spesial (Wajib, mis: `@, #, $`), Huruf Besar (`A-Z`), Huruf Kecil (`a-z`), dan Angka (`0-9`).<br>&nbsp;&nbsp;- Dilarang mengandung User ID, Nama Depan, atau Nama Belakang.<br>&nbsp;&nbsp;- Riwayat Password: Password baru tidak boleh sama dengan 10 riwayat password terakhir.<br>9. **Account Activation & Password Expiry Completion:** Setelah pembaruan password sukses, sistem mengubah `login_status = 'ACTIVE'`, `first_time_login_status = 'COMPLETED'`, mengosongkan `temporary_password_expired_at`, mengisi kolom `password_expired_at` dengan masa berlaku 90 hari (`CURRENT_TIMESTAMP + 90 hari`), memuat `mst_role_permissions` & `mst_role_menus`, serta membuat sesi di Redis Cache.<br>10. **Web POS Request Signature Verification Standard:** Selain permintaan Aktivasi (UC-WPOS-001), seluruh permintaan API yang dikirimkan oleh peramban Web POS WAJIB ditandatangani secara digital (*digital signature* - header X-Signature) menggunakan Private Key yang tersimpan pada *Local Device Storage* peramban (*IndexedDB / Hardware TPM*). API Gateway WAJIB memverifikasi signature tersebut menggunakan web_pos_public_key yang tersimpan pada tabel mst_merchant_terminals (atau Redis Cache). Apabila signature tidak valid, kedaluwarsa, atau missing, API Gateway WAJIB menolak request secara langsung (HTTP Status 401 INVALID_DEVICE_SIGNATURE).  |
| Expired Sesi OTP | 5 Menit |

# 2 Main Flow

**Tabel 2: Main Flow First Time Login User Kasir**

| No. | Aktivitas | Deskripsi |
| ---: | --- | --- |
| 1 | Membuka Aplikasi Web POS | Kasir membuka halaman login/aktivasi Web POS pada perangkat yang sudah ter-binding dan ter-aktivasi. |
| 2 | Memasukkan Kredensial Sementara | Kasir menginput email dan password sementara pada form aktivasi Kasir. |
| 3 | Mengirim Request Login | Kasir menekan tombol "Login", Front-End Web POS mengolah data dan mengirimkan request verifikasi sementara beserta data binding perangkat ke Auth Service via API Gateway. |
| 4 | Memvalidasi Binding & Kredensial | Auth Service memvalidasi status binding perangkat, email, hash password sementara, `login_type = 'REGULER'`, `login_status = 'INACTIVE'`, `first_time_login_status = 'REQUIRE_CHANGE_PASSWORD'`, keabsahan `merchant_id` & `store_id`, serta `temporary_password_expired_at > CURRENT_TIMESTAMP`. |
| 5 | Pengiriman Kode OTP via OTP Service | Auth Service memicu OTP Service untuk membuat 6-digit kode OTP dinamis (disimpan di Redis Cache) dan mengirimkannya ke email Kasir melalui Email Service. |
| 6 | Memasukkan Kode OTP | Kasir menerima email, lalu menginput 6-digit kode OTP pada form verifikasi Front-End Web POS. |
| 7 | Verifikasi OTP & Generasi Token Anti-Bypass | Auth Service memvalidasi kode OTP melalui OTP Service. Setelah cocok, Auth Service membuat `otp_verification_token` dinamis di Redis Cache (TTL 10m) dan mengembalikannya ke Front-End. |
| 8 | Memasukkan Password Baru | Kasir menginput password baru dan konfirmasi password sesuai kriteria standar CITRA. |
| 9 | Memvalidasi Password & Token Anti-Bypass | Front-End mengirimkan password baru beserta `otp_verification_token`. Auth Service memverifikasi keabsahan token anti-bypass di Redis Cache (lalu mengonsumsi/menghapusnya), serta menguji kriteria password CITRA dan riwayat 10 password terakhir. |
| 10 | Memperbarui Status Akun & Sesi | Auth Service memperbarui `mst_users` (`login_status = 'ACTIVE'`, `first_time_login_status = 'COMPLETED'`, `temporary_password_expired_at = NULL`, dan mengisi `password_expired_at = CURRENT_TIMESTAMP + 90 hari`), memuat `mst_role_permissions` & `mst_role_menus`, membuat token sesi (JWT) di Redis Cache, serta mencatat `audit_logs`. |
| 11 | Use Case Selesai | Front-End Web POS menyimpan token sesi dan menampilkan Layar Utama Transaksi (POS Main Screen) kepada Kasir. |

# 3 Alternative Flow

**Tabel 3: Alternative Flow First Time Login User Kasir**

| Kode | Alternative Flow | Deskripsi |
| --- | --- | --- |
| AF-01 | Perangkat Belum Ter-binding / Teraktivasi | Pada langkah 4, apabila perangkat Web POS belum ter-binding atau belum ter-aktivasi, Sistem menolak akses dan Front-End menampilkan `Perangkat Web POS belum ter-binding atau ter-aktivasi. Hubungi Admin.`. |
| AF-02 | Kredensial Sementara Salah / Kadaluarsa | Pada langkah 4, apabila password sementara salah atau `temporary_password_expired_at <= CURRENT_TIMESTAMP`, Sistem menolak akses dan Front-End menampilkan `Password sementara Kasir salah atau telah kedaluwarsa. Hubungi Supervisor/Admin.`. |
| AF-03 | Merchant ID / Store ID Tidak Valid | Pada langkah 4, apabila `merchant_id` atau `store_id` Kasir tidak terdaftar atau tidak sesuai, Sistem menolak login dan Front-End menampilkan `Data Merchant ID atau Store ID Kasir tidak valid.`. |
| AF-04 | Kode OTP Salah / Kadaluarsa | Pada langkah 7, apabila kode OTP yang diinput salah atau telah melewati batas 5 menit, OTP Service menolak verifikasi dan Front-End menampilkan `Kode OTP yang Anda masukkan salah atau telah kedaluwarsa.`. |
| AF-05 | Rate Limit Request OTP Terlampaui | Pada langkah 5, apabila permintaan ulang OTP melebihi 3 kali per menit, OTP Service menolak pengiriman dan Front-End menampilkan `Batas permintaan OTP terlampaui (maksimal 3 kali per menit). Silakan tunggu beberapa saat.`. |
| AF-06 | Indikasi Bypass OTP (Token Anti-Bypass Tidak Valid / Expired) | Pada langkah 9, apabila request pembaruan password dikirimkan tanpa `otp_verification_token` atau menggunakan token yang kadaluarsa/sudah terpakai, Sistem menolak permintaan dan Front-End menampilkan `Akses ditolak. Verifikasi OTP wajib dilakukan terlebih dahulu (Anti-Bypass Violation).`. |
| AF-07 | Password Baru Tidak Memenuhi Kriteria CITRA | Pada langkah 9, apabila password baru kurang dari 8 karakter, tidak memenuhi 3/4 kombinasi (karakter spesial wajib), atau mengandung nama/ID, Sistem menolak dan Front-End menampilkan `Password baru harus minimal 8 karakter dan memuat kombinasi kriteria CITRA.`. |
| AF-08 | Password Baru Sama dengan Riwayat 10 Password Terakhir | Pada langkah 9, apabila password baru pernah digunakan dalam 10 riwayat password terakhir, Sistem menolak dan Front-End menampilkan `Password baru tidak boleh sama dengan 10 riwayat password terakhir.`. |

# 4 Status Front-End

**Tabel 4: Status Front-End First Time Login User Kasir**

| No. | Nama Status | Deskripsi Tampilan Front-End |
| ---: | --- | --- |
| 1 | IDLE | Tampilan default form masukan email, password sementara, dan info toko/store ter-binding. |
| 2 | OTP_VERIFICATION | Tampilan modal/form masukan 6-digit kode OTP beserta tombol resend dan hitung mundur timer. |
| 3 | CHANGE_PASSWORD | Tampilan form input password baru dan konfirmasi password dilengkapi indikator kekuatan password standar CITRA. |
| 4 | LOADING | Tampilan indikator memuat (spinner) saat pemrosesan verifikasi kredensial, OTP, atau pembaruan password. |
| 5 | SUCCESS | Status sukses pembaruan password dan aktivasi akun sebelum halaman dialihkan ke Layar Transaksi POS. |
| 6 | ERROR | Tampilan pesan kesalahan (alert banner/toast) saat terjadi kesalahan kredensial, OTP salah, pelanggaran anti-bypass, atau password tidak sesuai kriteria. |

# 5 Activity Diagram First Time Login User Kasir

```plantuml
@startuml
title Activity Diagram - First Time Login User Kasir

|Kasir|
start
:Membuka aplikasi Web POS;
:Input email & password sementara;
:Klik "Login";

|Front-End|
:Kirim request login kredensial sementara & data binding;

|QRIS Engine|
:Validasi binding perangkat & kredensial sementara (INACTIVE);

if (Binding & kredensial sementara valid?) then (Tidak)
  |Front-End|
  :Tampilkan pesan error binding / kredensial sementara;
  stop
else (Ya)
  |QRIS Engine|
  :Generate OTP via OTP Service & kirim via Email Service;
  |Front-End|
  :Tampilkan form input OTP;
  |Kasir|
  :Input 6-digit kode OTP;
  |Front-End|
  :Kirim request verifikasi OTP;
  |QRIS Engine|
  :Validasi kode OTP via OTP Service;
  if (Kode OTP valid?) then (Tidak)
    |Front-End|
    :Tampilkan "Kode OTP salah atau kedaluwarsa";
    stop
  else (Ya)
    |QRIS Engine|
    :Generate & simpan otp_verification_token di Redis;
    |Front-End|
    :Tampilkan form pembaruan password baru;
    |Kasir|
    :Input password baru & konfirmasi (standar CITRA);
    |Front-End|
    :Kirim password baru + otp_verification_token;
    |QRIS Engine|
    :Validasi token anti-bypass & kriteria password CITRA;
    if (Token anti-bypass valid & password sesuai CITRA?) then (Tidak)
      |Front-End|
      :Tampilkan pesan error anti-bypass / kriteria password;
      stop
    else (Ya)
      |QRIS Engine|
      :Update mst_users (login_status = ACTIVE, first_time_status = COMPLETED, password_expired_at = +90 hari);
      :Consume token anti-bypass, generate JWT, & simpan sesi di Redis;
      |Front-End|
      :Simpan token sesi & tampilkan Layar Transaksi POS;
      |Kasir|
      :Masuk ke Layar Transaksi Web POS;
      stop
    endif
  endif
endif
@endl
```

# 6 Sequence Diagram First Time Login User Kasir

```plantuml
@startuml
title Sequence Diagram - First Time Login User Kasir

actor "Kasir" as user
boundary "Front-End" as fe
control "API Gateway" as gateway
control "Auth Service" as service
database "QRIS Database" as db
participant "Redis Cache" as redis
participant "OTP Service" as otp_svc
participant "Email Service" as email

user -> fe : Input email, temporary password & Klik "Login"
fe -> gateway : POST /api/v1/pos/auth/first-time-login/verify-temp (email, tempPassword, deviceBindingId)
gateway -> service : Teruskan permintaan login sementara Kasir

service -> service : Memvalidasi binding & aktivasi perangkat (deviceBindingId)

alt Perangkat Belum Ter-binding / Teraktivasi [AF-01]
  service --> gateway : 403XX00 (Perangkat Belum Ter-binding)
  gateway --> fe : 403XX00 (Perangkat Belum Ter-binding)
  fe --> user : Tampilkan "Perangkat belum ter-binding atau ter-aktivasi"
else Perangkat Ter-binding Valid
  service -> db : SELECT * FROM mst_users WHERE email = ? AND login_type = 'REGULER'
  db --> service : Return Data User (merchant_id, store_id, login_status, first_time_login_status, temporary_password_expired_at)
  
  alt Password Sementara Salah / Expired / Context Invalid [AF-02 / AF-03]
    service --> gateway : 401XX00 (Kredensial Sementara Invalid)
    gateway --> fe : 401XX00 (Kredensial Sementara Invalid)
    fe --> user : Tampilkan "Password sementara Kasir salah atau telah kedaluwarsa"
  else Kredensial Sementara Valid
    service -> redis : SETEX otp:{email} 300 {otp_code}
    redis --> service : OK
    service -> otp_svc : Request Generate & Send OTP (email, otp_code)
    otp_svc -> email : Trigger Delivery OTP Email (email, otp_code)
    email --> otp_svc : OK
    otp_svc --> service : Return OK
    service --> gateway : 200XX00 (OTP Sent Successfully)
    gateway --> fe : 200XX00 (OTP Sent Successfully)
    fe --> user : Tampilkan Form Input OTP
    
    user -> fe : Input 6-Digit OTP & Klik "Verifikasi OTP"
    fe -> gateway : POST /api/v1/pos/auth/first-time-login/verify-otp (email, otpCode)
    gateway -> service : Teruskan verifikasi OTP
    
    service -> otp_svc : Validate OTP (email, otpCode)
    otp_svc -> redis : GET otp:{email}
    redis --> otp_svc : Return otp_code
    otp_svc --> service : Return Validation Result
    
    alt Kode OTP Salah / Expired [AF-04]
      service --> gateway : 400XX00 (Kode OTP Invalid)
      gateway --> fe : 400XX00 (Kode OTP Invalid)
      fe --> user : Tampilkan "Kode OTP salah atau telah kedaluwarsa"
    else Kode OTP Valid
      service -> redis : DEL otp:{email}
      service -> redis : SETEX anti_bypass:{email} 600 {otp_verification_token}
      redis --> service : OK
      service --> gateway : 200XX01 (OTP Verified + Return otp_verification_token)
      gateway --> fe : 200XX01 (OTP Verified + Return otp_verification_token)
      fe --> user : Tampilkan Form Pembaruan Password Baru
      
      user -> fe : Input Password Baru & Klik "Simpan Password"
      fe -> gateway : PUT /api/v1/pos/auth/first-time-login/change-password (email, newPassword, otp_verification_token)
      gateway -> service : Teruskan permintaan ubah password
      
      service -> redis : GET anti_bypass:{email}
      redis --> service : Return stored_token
      
      alt Token Anti-Bypass Kosong / Tidak Cocok / Expired [AF-06]
        service --> gateway : 403XX01 (Anti-Bypass Violation)
        gateway --> fe : 403XX01 (Anti-Bypass Violation)
        fe --> user : Tampilkan "Akses ditolak. Verifikasi OTP wajib dilakukan terlebih dahulu"
      else Token Anti-Bypass Valid
        service -> service : Validasi Kriteria Password CITRA & Check 10 Password History
        alt Password Tidak Memenuhi Kriteria / Terdaftar di History [AF-07 / AF-08]
          service --> gateway : 422XX00 (Kriteria Password Invalid)
          gateway --> fe : 422XX00 (Kriteria Password Invalid)
          fe --> user : Tampilkan "Password baru tidak sesuai kriteria CITRA atau riwayat 10 password"
        else Password Baru Valid
          service -> redis : DEL anti_bypass:{email}
          service -> db : UPDATE mst_users SET password_hash = ?, login_status = 'ACTIVE', first_time_login_status = 'COMPLETED', temporary_password_expired_at = NULL, password_expired_at = CURRENT_TIMESTAMP + INTERVAL 90 DAY
          db --> service : OK
          service -> db : SELECT * FROM mst_role_permissions & mst_role_menus WHERE role_id = ?
          db --> service : Return Data Permissions & Structure Menu
          service -> redis : SETEX session:pos:{token} 900 {session_data_access_token_15m}
          redis --> service : OK
          service -> db : INSERT INTO audit_logs
          db --> service : OK
          service --> gateway : 200XX02 (First Time Login Success + Access Token 15m)
          gateway --> fe : 200XX02 (First Time Login Success + Access Token 15m)
          fe --> user : Redirect & Tampilkan Layar Transaksi Web POS
        end
      end
    end
  end
end
@endl
```

# 7 Response Code

**Tabel 5: Response Code First Time Login User Kasir**

| No. | HTTP Status | Response Code | Nama Response | Deskripsi | Respons Front-End |
| ---: | ---: | --- | --- | --- | --- |
| 1 | 200 | 200XX00 | POS_OTP_SENT_SUCCESS | Kredensial sementara valid dan kode OTP berhasil dikirimkan via OTP Service. | Menampilkan form masukan 6-digit kode OTP. |
| 2 | 200 | 200XX01 | POS_OTP_VERIFIED_SUCCESS | Verifikasi kode OTP berhasil melalui OTP Service dan token anti-bypass (`otp_verification_token`) diterbitkan. | Menampilkan form pengisian password baru. |
| 3 | 200 | 200XX02 | POS_FIRST_TIME_LOGIN_SUCCESS | Pembaruan password baru sukses, `password_expired_at` diisi 90 hari, akun Kasir berstatus `ACTIVE`, dan token sesi dibuat di Redis Cache. | Mengarahkan Kasir ke Layar Transaksi Web POS. |
| 4 | 400 | 400XX00 | INVALID_OTP_CODE | Kode OTP yang dimasukkan tidak cocok atau masa berlaku 5 menit telah kedaluwarsa pada OTP Service. | Menampilkan pesan kesalahan OTP tidak valid. |
| 5 | 401 | 401XX00 | INVALID_TEMP_CREDENTIALS | Kombinasi email atau password sementara Kasir salah, atau `temporary_password_expired_at` telah lewat. | Menampilkan pesan error kredensial sementara tidak valid. |
| 6 | 403 | 403XX00 | DEVICE_NOT_BOUND | Perangkat Web POS belum ter-binding atau ter-aktivasi untuk store_id terkait. | Menampilkan pesan error status binding perangkat. |
| 7 | 403 | 403XX01 | ANTI_BYPASS_VIOLATION | Permintaan pembaruan password dikirim tanpa `otp_verification_token` yang sah/terverifikasi. | Menampilkan pesan akses ditolak dan mewajibkan verifikasi OTP ulang. |
| 8 | 422 | 422XX00 | PASSWORD_CRITERIA_INVALID | Password baru tidak memenuhi kriteria standar CITRA (min 8 karakter, 3/4 kombinasi dengan karakter spesial wajib, dilarang mengandung ID/nama). | Menampilkan pesan error kriteria password. |
| 9 | 422 | 422XX01 | PASSWORD_HISTORY_VIOLATION | Password baru yang dimasukkan sama dengan salah satu dari 10 riwayat password terakhir. | Menampilkan pesan error riwayat password. |
| 10 | 429 | 429XX00 | OTP_RATE_LIMIT_EXCEEDED | Permintaan pengiriman ulang OTP melebihi batas 3 kali per menit pada OTP Service. | Menampilkan pesan peringatan rate limit OTP. |
| 11 | 500 | 500XX00 | INTERNAL_AUTH_ERROR | Gangguan internal pada layanan autentikasi / OTP Service / Email Service. | Menampilkan pesan kesalahan sistem internal. |

| 99 | 401 | 401XX01 | INVALID_DEVICE_SIGNATURE | Signature digital request Web POS (header X-Signature) tidak valid atau tidak cocok dengan web_pos_public_key pada mst_merchant_terminals. | Menampilkan pesan kesalahan Signature digital perangkat Web POS tidak valid atau tidak terverifikasi. |

# 8 Desain Antarmuka

**Tabel 6: Desain Antarmuka First Time Login User Kasir**

| Halaman | Komponen dan State Utama |
| --- | --- |
| Halaman Aktivasi Web POS | - Label **Nama Store / Outlet & Device ID** (Binding Info).<br>- Form Input **Email** & **Password Sementara** (`IDLE` / `ERROR`).<br>- Tombol Utama **"Login"** (`IDLE` / `LOADING`). |
| Modal / Step Verifikasi OTP | - Input 6-Digit **Kode OTP** (`OTP_VERIFICATION` / `ERROR`).<br>- Tombol **"Verifikasi OTP"** & Link **"Kirim Ulang OTP"** (dengan Countdown Timer). |
| Form Pembaruan Password Baru | - Form Input **Password Baru** & **Konfirmasi Password** (`CHANGE_PASSWORD`).<br>- Checklist Indikator Kriteria Password Standar CITRA.<br>- Tombol **"Simpan Password Baru"** (`IDLE` / `LOADING`). |
| Layar Utama Transaksi Web POS | - Header Info Kasir & Store (Menampilkan Nama Kasir, Email, `store_id`, `merchant_id`, & `role_id`).<br>- Menu & Fitur Transaksi Kasir (Dikelola berdasarkan `mst_role_menus` dan `mst_role_permissions`). |
